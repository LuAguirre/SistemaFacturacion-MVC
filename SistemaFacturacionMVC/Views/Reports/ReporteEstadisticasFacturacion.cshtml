@model IEnumerable<SistemaFacturacionMVC.Models.estadisticasFactura>

@{
    ViewData["Title"] = "ReporteEstadisticasFacturacion";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<script type="text/javascript">
    function mostrarDatos() {
        var fecha_inicio = document.getElementById("fecha-inicio").value;
        var fecha_final = document.getElementById("fecha-final").value;

        if (fecha_inicio != "" && fecha_final != "") {
            var url = "@Url.Action("ReporteEstadisticasFacturacion", "Reports", new { fechaInicio = "param-fecha1", fechaFinal = "param-fecha2" })";
            url = url.replace("param-fecha1", fecha_inicio).replace("param-fecha2", fecha_final);
            url = url.replace("amp;", "");
            $("#result").load(url);
            window.location.href = url;
        }
    }
</script>

<h1>Reporte de Estadisticas de Facturacion</h1>

<div class="form-group">
    <input type="date" id="fecha-inicio" onchange="mostrarDatos()" class="form-control" />
</div>

<div class="form-group">
    <input type="date" id="fecha-final" onchange="mostrarDatos()" class="form-control" />
</div>

<div class="table-responsive">
    <table class="table">
        <thead class="thead-dark">
            <tr style="text-align:center;">
                <th>
                    @Html.DisplayNameFor(model => model.Total_facturas)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.monto_facturado)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.total_Productos)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.dateInvoice)
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr style="text-align:center;">
                    <td>
                        @Html.DisplayFor(modelItem => item.Total_facturas)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.monto_facturado)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.total_Productos)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.dateInvoice)
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
